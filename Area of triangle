# Write the Area of a triangle using height and width 
h= float(input(' Please write the height of triangle: '))
w= float(input(' Please write the width of triangle: '))

# calculate area of triangle 
Area= w * h

print("\n Area of a Rectangle is: %.2f" %Area)
